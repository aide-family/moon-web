resolver ${NGINX_RESOLVER} valid=10s ipv6=off;

# 集成 / 微前端 all-in-one：按路径将 /v1 分发到各子应用后端（由 entrypoint envsubst 注入）

# --- Rabbit ---
location /v1/templates {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/template/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/email/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/webhook/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/sender/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/message-log {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/message-logs {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/alert-subscriptions {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/recipient-group {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}
location /v1/recipient-groups {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_RABBIT}";
    proxy_pass $upstream$request_uri;
}

# --- Marksman ---
location /v1/strategies {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/strategy/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/strategy-groups {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/strategy-group/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/levels {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/level/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/datasources {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/datasource/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/metric-query/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/metric/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}
location /v1/alert/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MARKSMAN}";
    proxy_pass $upstream$request_uri;
}

# --- Jade Tree ---
location /v1/ssh-commands {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_JADE_TREE}";
    proxy_pass $upstream$request_uri;
}
location /v1/ssh-command-audits {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_JADE_TREE}";
    proxy_pass $upstream$request_uri;
}
location /v1/probe-tasks {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_JADE_TREE}";
    proxy_pass $upstream$request_uri;
}
location /v1/machine-info {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_JADE_TREE}";
    proxy_pass $upstream$request_uri;
}
location /v1/machine-infos {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_JADE_TREE}";
    proxy_pass $upstream$request_uri;
}

# --- Main / Goddess（认证、账号、命名空间等，默认兜底）---
location /v1/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MAIN}";
    proxy_pass $upstream$request_uri;
}

location /oauth2/ {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MAIN}";
    proxy_pass $upstream$request_uri;
}

location /health {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    set $upstream "${API_UPSTREAM_MAIN}";
    proxy_pass $upstream$request_uri;
}
