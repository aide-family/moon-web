apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moon-web-marksman
  namespace: moon-web
  labels:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: marksman
spec:
  ingressClassName: nginx
  rules:
    - host: marksman.moon.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: moon-web-marksman
                port:
                  number: 80
