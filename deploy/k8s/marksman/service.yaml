apiVersion: v1
kind: Service
metadata:
  name: moon-web-marksman
  namespace: moon
  labels:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: marksman
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: marksman
  ports:
    - name: http
      port: 80
      targetPort: http
