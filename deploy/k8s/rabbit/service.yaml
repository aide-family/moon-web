apiVersion: v1
kind: Service
metadata:
  name: moon-web-rabbit
  namespace: moon
  labels:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: rabbit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: rabbit
  ports:
    - name: http
      port: 80
      targetPort: http
