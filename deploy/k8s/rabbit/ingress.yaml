apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moon-web-rabbit
  namespace: moon-web
  labels:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: rabbit
spec:
  ingressClassName: nginx
  rules:
    - host: rabbit.moon.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: moon-web-rabbit
                port:
                  number: 80
