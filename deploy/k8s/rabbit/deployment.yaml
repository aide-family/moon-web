apiVersion: apps/v1
kind: Deployment
metadata:
  name: moon-web-rabbit
  namespace: moon
  labels:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: rabbit
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: moon-web
      app.kubernetes.io/component: rabbit
  template:
    metadata:
      labels:
        app.kubernetes.io/name: moon-web
        app.kubernetes.io/component: rabbit
    spec:
      containers:
        - name: web
          image: ghcr.io/aide-family/moon-web/rabbit
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          env:
            - name: MOON_APP_NAME
              value: rabbit
            - name: API_UPSTREAM
              value: http://rabbit.moon.svc.cluster.local:8001
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
