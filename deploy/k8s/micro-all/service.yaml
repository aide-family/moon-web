apiVersion: v1
kind: Service
metadata:
  name: moon-web-all
  namespace: moon-web
  labels:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: all
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: moon-web
    app.kubernetes.io/component: all
  ports:
    - name: http
      port: 80
      targetPort: http
